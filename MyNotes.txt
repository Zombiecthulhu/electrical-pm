Password hard coded in the backend .env file?
Material-UI Grid API - The grid componentapi has changed in newer versions
Take out CORS being more permissive for mobile before producton

***************************************************
RECOMMENDED: Use the PowerShell scripts to prevent zombie processes:
  .\start-servers.ps1     - Start both servers (cleans ports first)
  .\stop-servers.ps1      - Stop both servers cleanly
  .\restart-servers.ps1   - Restart both servers

MANUAL START (if needed):
  cd "C:\Users\johnj\Desktop\Project Management App\backend"
  npm run dev
  
  cd "C:\Users\johnj\Desktop\Project Management App\frontend"
  npm start

EMERGENCY CLEANUP (if servers won't start):
  taskkill /F /IM node.exe

DATABASE MANAGEMENT:
  cd "C:\Users\johnj\Desktop\Project Management App\backend"
  npx prisma studio

URLs:
  Backend:  http://localhost:5000
  Frontend: http://localhost:3000
  Prisma:   http://localhost:5555