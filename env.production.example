# Production Environment Variables Template
# INSTRUCTIONS:
# 1. Copy this file to .env.production on your Synology NAS
# 2. Replace all placeholder values with your actual values
# 3. Keep this file secure - never commit .env.production to git

# Database Password (use a strong, random password)
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# JWT Secrets (use long random strings - at least 64 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING
REFRESH_TOKEN_SECRET=CHANGE_THIS_TO_ANOTHER_RANDOM_64_CHAR_STRING

# URLs - Update these with your domain or NAS IP
# If using Cloudflare Tunnel: https://electrical-pm.yourdomain.com
# If using local network: http://192.168.1.XXX:3001
FRONTEND_URL=http://YOUR_NAS_IP:3001
BACKEND_URL=http://YOUR_NAS_IP:5001

