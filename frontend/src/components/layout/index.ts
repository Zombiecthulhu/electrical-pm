/**
 * Layout Components Index
 * 
 * This file exports all layout components for the app shell.
 * Layout components define the overall structure and navigation of the application.
 * 
 * Layout components:
 * - AppShell (main layout wrapper)
 * - Header/TopBar (navigation, user menu)
 * - Sidebar (module navigation)
 * - Footer
 * - Breadcrumbs
 * - PageHeader (page title, actions)
 * 
 * Export pattern:
 * export { AppShell } from './AppShell';
 * export { Header } from './Header';
 * export { Sidebar } from './Sidebar';
 */

// Layout components will be exported here as they are created
export { default as AppLayout } from './AppLayout';
export { MobileBottomNav } from './MobileBottomNav';

