/**
 * Theme Index
 * 
 * This file exports Material-UI theme configuration.
 * Customize the MUI theme to match your brand and design requirements.
 * 
 * Theme configuration:
 * - theme.ts         - Main theme object
 * - colors.ts        - Color palette (future)
 * - typography.ts    - Font and text styles (future)
 * - spacing.ts       - Spacing scale (future)
 * - breakpoints.ts   - Responsive breakpoints (future)
 * - components.ts    - Component style overrides (future)
 * 
 * Theme features:
 * - Professional blue color scheme
 * - Gray secondary colors
 * - Typography scale
 * - Spacing system (8px base)
 * - Component style overrides
 * - Responsive breakpoints
 * - Custom shadows
 * 
 * Usage:
 * import { theme } from '@/theme';
 * import { ThemeProvider } from '@mui/material/styles';
 * 
 * <ThemeProvider theme={theme}>
 *   <App />
 * </ThemeProvider>
 */

export { default as theme } from './theme';
export { default } from './theme';

